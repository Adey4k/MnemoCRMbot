// перший запуск бота або /start
module.exports = (bot) => {
  bot.start((ctx) => {
    ctx.reply(
        "Привіт! Введіть /help щоб отримати загальну інформацію про бота"+
    "і довідку по командам, або /add щоб одразу додати перший запис!",
    );
  });
};
